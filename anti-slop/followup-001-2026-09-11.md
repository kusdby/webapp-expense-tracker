# Follow-up 001: approved finance audit fixes

2026-09-11. Scope: findings 1 through 6 from audit-001-2026-09-11.md. Existing vanilla JavaScript and user-directed light pastel design retained; no React migration, new public service, skill installation, scanner bypass, or production CRUD.

## Implemented and unit-tested

1. Period changes clear old transaction rows before fetching the new list; a failed list request leaves a scoped error, never old rows under new-period metrics. The audit-approved clear-and-error alternative is used, not an all-or-nothing backend transaction.
2. Authentication and summary failures are distinguished by HTTP status. Authenticated summary errors retain the dashboard with Coba lagi; 401 returns to login.
3. Initial metric values are nonnumeric placeholders, with dashboard loading text and aria-busy until the summary/list request finishes.
4. Filter flex items and selects can shrink and wrap. Long account names no longer impose their intrinsic width on the tablet layout by CSS inspection; current-build browser geometry remains unverified.
5. Failed submit restores the active field/control. Nested edit completion returns focus inside the remaining search dialog. Resumption discovered a failing nested-save test: document order was incorrectly treated as modal stacking order. Action scope now comes from the active element's dialog and is retained while that dialog stays open.
6. Positive integer amount validation identifies the amount field and explains values must exceed zero. Read errors name the read action without a duplicate-save warning. Empty local results distinguish active filters and selected period.

## Actual execution evidence

- Initial resumed jsdom suite: 12 passed, 1 failed (nested failed-save focus).
- After the focused-dialog fix: node --test tests/ui-behavior.cjs, 13 passed, 0 failed.
- python3 -m unittest discover -s tests -v: 30 passed.
- python3 scripts/smoke_auth.py: `smoke ok 3525000 25 3`, using the separate dev-smoke-auth.db, not production finance.db.
- Commands run from the project directory. jsdom uses node:22-bookworm-slim and the existing read-only ui-design-kit dependency mount. No installs were required.
- Prepared reproducible Chromium assertions for all six findings in /DATA/fin-approved-browser.js, using /DATA/fin-reaudit-cdp.mjs and the existing isolated synthetic fixture. These assertions have NOT executed successfully on this resumption and are not evidence of a browser pass.

## Blocking approvals and delivery status

The tool returned pending_approval for `docker restart fin-reaudit-fixture` (container lifecycle), and for an attempt to refresh only the fixture's temporary copied web assets plus invoke the existing CDP runner (reported recursive delete). Neither was executed. No alternate path was used to bypass these approvals.

Therefore current-build Chromium validation and deployment remain BLOCKED. No new deployment ID is claimed. The existing production container is unchanged by this work. No production database backup, deployment-time digest comparison, or live asset verification has been performed on this resumption. The deploy helper was inspected: it reads stale /DATA/fin-deploy-private/container.json and uses a fixed backup filename. Do not run it unchanged. After approval, capture the current container config privately, use a timestamped private SQLite backup, preserve current env/mounts, then verify full SQLite logical digest and served/container assets.

This is a partial verification report, not a complete Delivery Gate PASS. Real-browser keyboard/overflow checks of the current source, screen reader, zoom 200%, every contrast pairing, and deployment remain unverified. Standalone anti-slop concerns and ui-design-kit were loaded; the installed core was also exposed by skill lookup, but its installation/configuration instructions were not executed and no full-suite certification is asserted.

## Design reasons

Pastel yellow balance, peach expense, and lavender income remain because the user chose them to distinguish financial metrics. Existing layout, typography, spacing, and elevation were retained to avoid a redesign. Changes are limited to truthful states, responsive filter sizing, clear action feedback, and keyboard recovery.
